# 📊 Angular Dashboard

## Overview
A dynamic, interactive dashboard built with **Angular 20** that visualizes mock sales and user data using **charts, tables, and KPI cards**.  
Users can **drag, resize, and customize** widgets — and their layout is saved in `localStorage` for persistence.

---

## ✨ Features

- Grid-based responsive layout using **Gridster2**.  
- Widgets are **draggable** and **resizable**.  
- Layout saved automatically or manually via **Save Layout**.  
- **KPI Card (Total Sales)** – Displays key metrics.  
- **Charts (Line/Bar/Pie)** – Switchable via dropdown.  
- **Data Table** – Sort, filter, and paginate.  
- **Filter Bar** – Global dataset and date filters.  
- Layout persistence via `localStorage`.  
- **Export CSV** support.  

---

## 🧰 Tech Stack
- **Angular 20**  
- **Angular Material (M2)**  
- **Chart.js / ng2-charts**  
- **Gridster2**  
- **SCSS**

---

## 🚀 Setup

```bash
# Install dependencies
npm install

# Run locally
ng serve

# Open in browser
http://localhost:4200/
