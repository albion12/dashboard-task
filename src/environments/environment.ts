export const environment = {
  production: false,
  // Backend API base URL
  // If your backend uses an /api prefix, change to: 'http://localhost:3000/api'
  // Adjust port (3000) if your backend runs on a different port
  apiBaseUrl: 'http://localhost:3000'
};
